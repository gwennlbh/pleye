// Supported shortcuts:
// - PR Number: resolves to a PR's branch and goes to /branches/[branch]
// - Workflow Run/Job ID: goes to (TODO) /runs/[id]
// - A branch name: goes to /branches/[branch]
// - A filename: goes to (TODO) /[...filepath]
// - Other: (TODO) searches

export async function GET({params}) {

}
